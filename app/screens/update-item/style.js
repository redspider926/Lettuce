import {StyleSheet} from 'react-native';
import * as colors from 'config/colors';

export default StyleSheet.create({
  photoContainer: {
    width: '100%',
    height: 170,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 20,
    backgroundColor: colors.disactiveGray,
  },
});
