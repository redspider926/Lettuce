import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  bottomSheetItemContainer: {
    flex: 1,
    display: 'flex',
    flexDirection: 'row',
    paddingLeft: 20,
    alignItems: 'center',
  },
});
